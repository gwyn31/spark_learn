CREATE TABLE test.FAKELOG(
	LOGID CHAR(32) NOT NULL UNIQUE PRIMARY KEY,
    ACQ_NUM INT NOT NULL DEFAULT 0,
    DEQ_NUM INT NOT NULL DEFAULT 1,
    ACQ_SHARE DECIMAL(5, 2) NOT NULL DEFAULT 0.0,
    ITEM_CODE VARCHAR(31) NOT NULL DEFAULT '',
    STATUS_FLAG CHAR(1) NOT NULL DEFAULT 'B',
    LOGTIME DATETIME NOT NULL,
    KEY TIME_IDX(LOGTIME)
);